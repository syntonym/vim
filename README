My configuration of vim, so i can clone it to all my workingstations (and use submodules to bind in plugins)

CC-BY (if configs are able to be licensed at all)
~~Syntonym

git clone https://github.com/gmarik/Vundle.vim.git ~/.vim/bundle/Vundle.vim
